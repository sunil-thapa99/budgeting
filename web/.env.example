# Supabase browser keys (see DB/README.md). The anon key is safe in the client.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...

# Backend origin for production (leave unset in dev — Vite proxies /api to :5174).
# VITE_API_URL=https://your-backend.onrender.com
