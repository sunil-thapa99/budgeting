{
  "name": "budgeting",
  "private": true,
  "version": "0.1.0",
  "description": "Personal expense/savings tracker with NVIDIA-LLM insights and receipt scanning",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
